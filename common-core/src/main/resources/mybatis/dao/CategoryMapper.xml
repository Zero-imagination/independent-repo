<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.disagreed.independentrepo.repository.mybatis.CategoryMapper">

    <resultMap id="Category" type="CategoryEntity" autoMapping="true">
        <id property="categoryId" column="category_id"/>
    </resultMap>

    <sql id="AliasBaseCategoryColumns">
        ${alias}.category_id as ${prefix}category_id,
        ${alias}.name as ${prefix}name,
        ${alias}.description as ${prefix}description
    </sql>

    <sql id="BaseCategoryColumns">
        <include refid="AliasBasePersonColumns">
            <property name="alias" value="cg"/>
            <property name="prefix" value=""/>
        </include>
    </sql>

</mapper>